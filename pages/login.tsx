import React from 'react';
import LoginForm from '../components/AuthForm/index';
import Layout from '../components/Layout';

const login = (): JSX.Element => (
  <Layout>
    <LoginForm />
  </Layout>
);

export default login;
